#include <stdio.h>
/**
 * main - Entry point into the program
 * 
 * Return: 0 if successful, other numbers otherwise
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}

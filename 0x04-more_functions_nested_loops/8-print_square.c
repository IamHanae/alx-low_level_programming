#include "main.h"

/**
 * print_square - Prints a square
 * followed by a new line
 * Return: Void
 */
void print_square(int size)
{
	int i, j;

	for (i = 0; i < n; i++)
	{
		for (j = 0; j < n; j++)
		{
			_putchar('#');
		}
		_putchar('\n');
	}
}

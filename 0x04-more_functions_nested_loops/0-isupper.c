#include "main.h"

/**
 * _isupper - Checks for uppercas character
 * @c: The number to check
 * Return: 1 if uppercase. 0 otherwise
 */
int _isupper(int c)
{
	int res;

	if (c >= 'A' && c <= 'Z')
		res = 1;
	else
		res = 0;

	return (r);
}
